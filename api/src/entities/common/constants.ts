export enum StatusType {
  ACTIVE = "active",
  INACTIVE = "inactive",
  ARCHIVED = "archived",
  DELETED = "deleted",
  VENDIDO = "vendido",
  REVISION = "revision",
}

export enum OrderStatus {
  IN_REVIEW = "in review",
  SOLD = "sold",
}
